{
  "name": "@agiterra/wallet-extension-monorepo",
  "version": "0.4.0-dev.0",
  "private": true,
  "description": "Workspace root for @agiterra/wallet-extension (prod, with Wire) and @agiterra/wallet-extension-ci (CI, no Wire). Shared core in packages/core.",
  "workspaces": [
    "packages/core",
    "packages/prod",
    "packages/ci"
  ],
  "scripts": {
    "build": "bun --filter @agiterra/wallet-extension build && bun --filter @agiterra/wallet-extension-ci build",
    "build:prod": "bun --filter @agiterra/wallet-extension build",
    "build:ci": "bun --filter @agiterra/wallet-extension-ci build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "bun-types": "latest",
    "typescript": "^6.0.3"
  },
  "license": "MIT",
  "author": "Tim Garthwaite <https://github.com/mividtim>"
}
