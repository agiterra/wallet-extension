node_modules/
dist/
*.log
.DS_Store

# Agent/tooling working-tree artifacts — not part of the extension source.
# (Swept into PR #4 by a stray `git add -A`; ignore so it can't recur.)
.claude/
report/
